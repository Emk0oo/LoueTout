<nav class="border-b-2 border-gray-200">
    <div class="max-w-screen-xl flex items-center justify-between mx-auto p-4">

        <a href="./" class="flex items-center text-xl font-bold">
            {{ current_instance().getSetting('name') }}
        </a>

        <div class="flex space-x-4">
            <input class="flex items-center px-2 py-2 rounded-lg bg-stone-100 hover:bg-stone-200" placeholder="Search"></input>
            <button class="flex items-center px-4 py-2 rounded-lg bg-stone-100 hover:bg-stone-200 font-bold">Log in</button>
            <button class="flex items-center px-4 py-2 rounded-lg bg-stone-100 hover:bg-stone-200">Cart</button>
        </div>
    </div>
</nav>
