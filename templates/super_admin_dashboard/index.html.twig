{% extends 'super_admin_base.html.twig' %}

{% block title %}Admin dashboard{% endblock %}

{% block body %}

<a href="{{ path('app_super_admin_add_instance') }}" class="btn btn-primary">Créer une instance</a>

<h1>Liste des instances</h1>
<ul>
    {% for instance in instanceList %}
        <li>{{ instance.name }}</li>
    {% endfor %}
</ul>

{% endblock %}
